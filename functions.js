//function declaration
//function functionName(parameters) {
    //  code to be executed
    //}

  //  function greet() {
    //    console.log("Hello, " + Ajibola + "!");
  //  }
    //greet();


    function fareWell(name) {
        console.log("see you later, " + name + "!")
    }

    fareWell("Ajibola");

    let x = 5;
    let y = 10;
    console.log(x + y);

 function add (){
    let a = 5;
    let b = 10;
    return a + b;

 }
    console.log(add());

    function addition(a, b) {
        return a + b;
    }
    console.log(addition(7, 5));

    function isAdult(age) {
        return age >= 18;
    }
    console.log(isAdult(20)); // true
    console.log(isAdult(15)); // false

    
//return a static value is a function that always returns the same value regardless of the input. For example:
//function getPi() {
  //  return 3.14159;
//}
//console.log(getPi()); // Output: 3.14159

//return a dynamic value is a function that returns a value based on the input or some internal logic. For example: